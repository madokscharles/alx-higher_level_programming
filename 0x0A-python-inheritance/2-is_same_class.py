#!/usr/bin/python3
""" Checks if true or false """


def is_same_class(obj, a_class):
    """ Returns true if object is an instance
    of the class else returns false
    """
    return (type(obj) == a_class)
